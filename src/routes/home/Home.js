import React, { useState, useEffect } from 'react';
import useTitle from '../../hooks/useTitle';

const Home = () => {
  useTitle('');

  return (
    <div>     
      Home...
    </div>
    
  )
}

export default Home;